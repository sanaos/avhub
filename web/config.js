// config.js
const API_CONFIG = {
    BASE_URL: '/api/v1',
    ENDPOINTS: {
        SEARCH: '/avcode',
        COLLECTIONS: '/hacg',
        VIDEO: '/get_video',
        HOT_SEARCHES: '/hot_searches'
    }
};

// 导出配置
export default API_CONFIG;
